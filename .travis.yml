language: cpp
arch: amd64
os: linux 
dist: bionic

before_install:
    - sudo apt-get update
    - sudo apt-get install -y python build-essential zlib1g-dev xz-utils clang-3.8
    - sudo chmod +x ./install-deps.sh && sudo ./install-deps.sh

compiler:
  - clang

# Build steps
script: 
  - cd ${TRAVIS_BUILD_DIR}
  - cmake . 
  - make
  - ./tests/VarIntTest


after_success:
- ls
- echo "done"


